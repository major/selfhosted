---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: flux-system
resources:
  # 🔐 Sealed secret for Discord webhook (create this first!)
  - discord-webhook-sealed-secret.yaml
  # 📢 Discord provider configuration
  - provider.yaml
  # 🚨 Alert definitions (INFO level - adjust if too noisy)
  - alerts.yaml
