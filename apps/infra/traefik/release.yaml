---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: traefik
  namespace: traefik
spec:
  interval: 30m
  chart:
    spec:
      chart: traefik
      version: '37.1.2'
      sourceRef:
        kind: HelmRepository
        name: traefik
        namespace: traefik
      interval: 12h
  values:
    deployment:
      replicas: 1

    # Port configuration
    ports:
      web:
        port: 80
        expose:
          default: true
        exposedPort: 80
        # Redirect HTTP to HTTPS
        redirections:
          entryPoint:
            to: websecure
            scheme: https
            permanent: true
      websecure:
        port: 443
        expose:
          default: true
        exposedPort: 443
        tls:
          enabled: true

    # Persistence disabled (no longer needed for ACME)
    persistence:
      enabled: false

    # Service configuration
    service:
      type: LoadBalancer

    # Enable dashboard (accessible via custom IngressRoute)
    ingressRoute:
      dashboard:
        enabled: false

    # Logging
    logs:
      general:
        level: INFO
      access:
        enabled: true
