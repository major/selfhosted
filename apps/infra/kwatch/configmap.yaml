---
apiVersion: v1
kind: ConfigMap
metadata:
  name: kwatch
  namespace: kwatch
data:
  config.yaml: |
    # 🔔 kwatch Configuration

    # 📊 General settings
    maxRecentLogLines: 50
    ignoreFailedGracefulShutdown: true

    # 💬 Discord notifications
    alert:
      discord:
        webhook: ${DISCORD_WEBHOOK_URL}
        title: "🚨 K3s Alert"
        text: "Cluster event detected"

    # 🎯 Monitoring filters (optional - leave empty to monitor all)
    # namespaces:
    #   - default
    #   - production

    # reasons:
    #   - BackOff
    #   - CrashLoopBackOff

    # ignoreContainerNames:
    #   - istio-proxy
