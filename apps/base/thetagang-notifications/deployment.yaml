---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: thetagang-notifications
  namespace: thetagang
  labels:
    app: thetagang-notifications
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: thetagang-notifications
  template:
    metadata:
      labels:
        app: thetagang-notifications
    spec:
      containers:
        - name: thetagang-notifications
          image: ghcr.io/major/thetagang-notifications:main@sha256:79d00ef6e292b8222e7c08e10471d31266d54a964b74abebc0beb2ff7bae38c2
          imagePullPolicy: Always
          securityContext:
            readOnlyRootFilesystem: true
          env:
            # 🔐 Sensitive credentials from SealedSecret
            - name: WEBHOOK_URL_TRADES
              valueFrom:
                secretKeyRef:
                  name: thetagang-secrets
                  key: webhook-url-trades
            - name: TRADES_API_KEY
              valueFrom:
                secretKeyRef:
                  name: thetagang-secrets
                  key: trades-api-key

            # ⚙️ Application configuration
            - name: DAEMONIZE_TRADE_BOT
              value: "1"
            - name: PATRON_TRADES_ONLY
              value: "1"
            - name: STORAGE_DIR
              value: "/storage"
            - name: SKIPPED_USERS
              value: "antithetagang,jrue"

            # 🔗 Redis connection (cross-namespace communication)
            - name: REDIS_HOST
              value: "redis.redis-system.svc.cluster.local"
            - name: REDIS_PORT
              value: "6379"

          volumeMounts:
            - name: storage
              mountPath: /storage

          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"

      volumes:
        - name: storage
          persistentVolumeClaim:
            claimName: thetagang-storage
