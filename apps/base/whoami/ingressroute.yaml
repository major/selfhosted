---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: whoami-secure
  namespace: default
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`whoami.amajor.cloud`)
      kind: Rule
      services:
        - name: whoami
          port: 80
  tls: {}
