---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: adminer
  namespace: postgres
  labels:
    app: adminer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: adminer
  template:
    metadata:
      labels:
        app: adminer
    spec:
      containers:
        - name: adminer
          image: adminer:4.17.1@sha256:c00645aed0a46a37917ea7411afabbfa41aa8b4e75a5e4bd7448ceb904dd37a7
          ports:
            - containerPort: 8080
              name: http
          env:
            # Set design to a nicer theme
            - name: ADMINER_DESIGN
              value: "pepa-linha"
            # Default server (can be overridden in UI)
            - name: ADMINER_DEFAULT_SERVER
              value: "postgres-rw"
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              memory: 128Mi
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
